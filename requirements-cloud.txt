# Storibot 2026 Outreach Platform — Cloud Deployment Requirements
# Compatible with Replit, PythonAnywhere, Render, Heroku, Fly.io

# Core AI — Anthropic SDK with tool_use, prompt caching, and managed agents support
anthropic>=0.50.0

# Web framework
fastapi==0.110.0
uvicorn==0.29.0

# Data validation
pydantic==2.6.4
python-dotenv==1.0.1

# That's it — 5 packages, no Docker, no database required for cloud deployment
